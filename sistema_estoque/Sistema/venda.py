class Venda:

    def __init__(self, id, cliente_id, produto_id, quantidade, valor_total):
        self.id = id
        self.cliente_id = cliente_id
        self.produto_id = produto_id
        self.quantidade = quantidade
        self.valor_total = valor_total